---
import Container from "./Container.astro";
import { courses } from "../data/courses";
---

<div id="blog">
  <Container>
    <div class="mb-12 space-y-2 text-center">
      <h2 class="text-3xl font-bold text-gray-800 md:text-4xl dark:text-white">
        Nossos cursos
      </h2>
      <p class="lg:mx-auto lg:w-6/12 text-gray-600 dark:text-gray-300">
        Já temos vários cursos disponíveis para você, e estamos sempre lançando
        novos cursos, então fique ligado!
      </p>
    </div>
    <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
      {
        courses.map((course) => (
          <div class="group p-6 rounded-3xl bg-white border border-gray-100 dark:shadow-none dark:border-gray-700 dark:bg-gray-800 bg-opacity-50 shadow-2xl shadow-gray-600/10">
            <div class="relative overflow-hidden rounded-xl">
              <img
                src={course.image}
                alt="art cover"
                loading="lazy"
                width="1000"
                height="667"
                class="h-64 w-full object-cover object-top transition duration-500 group-hover:scale-105"
              />
            </div>
            <div class="mt-6 relative">
              <h3 class="text-2xl font-semibold text-gray-800 dark:text-white">
                {course.title}
              </h3>
              <p class="mt-6 mb-8 text-gray-600 dark:text-gray-300">
                {course.description}
              </p>
              <div class="flex items-center justify-between">
                <a class="inline-block" href={`/curso/${course.id}`}>
                  <span class="text-info text-primary dark:text-primary">
                    Mais detalhes
                  </span>
                </a>
                <a
                  href={course.link}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="inline-block bg-primary hover:bg-secondary text-white font-semibold py-2 px-4 rounded-md shadow-md transition-all duration-300"
                >
                  Comprar
                </a>
              </div>
            </div>
          </div>
        ))
      }
    </div>
  </Container>
</div>
